let mongoose;_f90‍.x([["default",()=>_f90‍.o]]);_f90‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;

const PostSchema = new Schema({
    title: String,
    body: String,
    tags: [String],
    publishedDate: {
        type: Date,
        default: Date.now,
    },
    user:{
        _id:mongoose.Types.ObjectId,
        username: String
    },
})

const Post = mongoose.model('Post', PostSchema);
_f90‍.d(Post);